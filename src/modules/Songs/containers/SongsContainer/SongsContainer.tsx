import { COLORS } from '@cookup/constant'
import { Layout } from '@cookup/components'

const SongsContainer = () => {
  return <Layout bgcolor={COLORS.background}>SongsContainer</Layout>
}

export default SongsContainer
